version: '3.8'

services:
  anpta_apple:
    build:
      context: .
      dockerfile: anpta_apple/Dockerfile
    image: anpta_apple:latest
    container_name: anpta_apple
    volumes:
     - .:/data/
    command: /bin/bash
    tty: true
    stdin_open: true
